---
phase: 02-frontend-admin-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - internal/api/handler_events.go
  - internal/api/router.go
  - web/admin/index.html
  - web/admin/admin.js
  - web/admin/admin.css
autonomous: true
requirements:
  - UI-06
  - UI-07

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GET /queue/events returns {events: [...]} with eventIds discovered via Redis SCAN — never KEYS (D-07/D-09)"
    - "Admin dashboard at http://localhost:8082/admin/ loads an event selector dropdown auto-populated from GET /queue/events (D-07)"
    - "Selecting an event starts 2-second polling of GET /queue/config/:eventId and populates all 6 stat cards (D-05/UI-06)"
    - "Submitting rate and capacity inputs fires PUT /queue/rate/:eventId and shows 'Saved' for 1.5s then reverts to 'Update Config' (UI-07)"
    - "When GET /queue/events returns [], selector shows 'No events active' and stat cards display '—' (empty state per UI-SPEC)"
    - "Admin page uses window.QUEUE_CONFIG.apiBase — no hardcoded URLs (D-02)"
    - "CORS allows admin dashboard origin http://localhost:8082 (already wired in Plan 02-01)"
  artifacts:
    - "internal/api/handler_events.go: GetEvents handler using Redis SCAN on queue:* keys"
    - "internal/api/router.go: GET /queue/events route registered"
    - "web/admin/index.html: admin dashboard with event selector, 6 stat cards, rate/capacity form"
    - "web/admin/admin.js: full polling loop, event selector, PUT update with success/failure states"
    - "web/admin/admin.css: two-region layout, stat grid, form styling per UI-SPEC"
  key_links:
    - "admin.js loadEvents() → GET /queue/events → populates event-select dropdown"
    - "admin.js startPolling(eventId) → GET /queue/config/:eventId every 2s → renderStats()"
    - "admin.js submit handler → PUT /queue/rate/:eventId with {rate, capacity} → 'Saved'/'Update failed' states"
    - "handler_events.go GetEvents → h.rdb.Scan(ctx, cursor, 'queue:*', 100) → strips 'queue:' prefix"
  prohibitions:
    - "GET /queue/events MUST use SCAN not KEYS — KEYS blocks Redis on large keyspaces (D-07 specifics)"
    - "eventIds slice MUST never be null in the JSON response — return empty array [] when no events found"
    - "Admin dashboard MUST NOT implement authentication — explicitly deferred (D-06)"
    - "Headroom stat MUST be computed client-side as capacity - activeUsers — do NOT add a server-side headroom field"
    - "Stat cards MUST display '0' as the string '0', not '—' or empty — zero is a valid operational value (UI-SPEC zero-one-many)"

planner_assumptions:
  - "UI-06 empty state edge: when GET /queue/events returns [], event selector shows 'No events active' option (value=''), polling loop does NOT start. Stat card values display '—'. This is a valid stable state — no error needed."
  - "UI-06 ordering edge: events returned by SCAN are in arbitrary order (Redis SCAN has no ordering guarantee). No sort required — order is non-deterministic and acceptable for an ops dropdown."
  - "UI-07 boundary: rate input min=1 enforced by HTML input min attribute and parseInt validation before PUT. A rate of 0 is technically valid (scheduler pause), but the UI should enforce min=1 to match the input element spec (UI-SPEC). If the user wants rate=0 they can use the API directly."
  - "UI-06 stale data: when GET /queue/config fails (non-200 or network error), stat values retain the last known value with '(stale)' suffix appended. This is the exact resolution from UI-SPEC State Coverage — implement literally."
  - "Headroom display: when capacity is 0 (not configured), headroom displays '—' not a negative number. Guard: if capacity === 0 show '—' else show (capacity - activeUsers). Accent color (#2563eb) on Headroom card value only when headroom < 10% of capacity per UI-SPEC layout contract."
  - "UI-SPEC backstop: stat card values at extreme scale (queue depth = 500,000) must not overflow the card boundary. Add CSS overflow-wrap: break-word and min-width: 0 on stat value elements."
---

<objective>
Deliver the admin dashboard: a new Go endpoint discovers active events via Redis SCAN, and the admin HTML/JS provides live stat polling and rate/capacity updates. After this plan, an operator can select an active event and see live queue metrics.

Purpose: Enables live operational control (UI-06, UI-07) — operators can monitor queue depth and adjust admit rate without touching Redis directly.
Output: Working admin dashboard at http://localhost:8082/admin/ with event selector, 6 live stat cards, and rate/capacity update form.
</objective>

<execution_context>
@/Users/aditya.bansal@zomato.com/projects/virtual-queue/.claude/gsd-core/workflows/execute-plan.md
@/Users/aditya.bansal@zomato.com/projects/virtual-queue/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-frontend-admin-ui/02-CONTEXT.md
@.planning/phases/02-frontend-admin-ui/02-UI-SPEC.md
@.planning/phases/02-frontend-admin-ui/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET /queue/events Go handler and route registration</name>
  <files>internal/api/handler_events.go, internal/api/router.go</files>
  <read_first>
    - internal/api/handler_admin.go (exact Handler receiver pattern, ctx := context.Background() pattern, error response pattern)
    - internal/api/router.go (current route list — add GET /queue/events after existing routes)
    - .planning/phases/02-frontend-admin-ui/02-PATTERNS.md (GetEvents implementation pattern with SCAN loop, strip "queue:" prefix logic)
  </read_first>
  <action>
    **internal/api/handler_events.go:** New file in package api. Implement GetEvents(c *gin.Context) on *Handler receiver.

    Imports: "context", "net/http", "github.com/gin-gonic/gin" — same as handler_admin.go.

    GetEvents logic:
    - ctx := context.Background()
    - Declare var cursor uint64 = 0 and var eventIDs []string
    - SCAN loop: for { keys, next, err := h.rdb.Scan(ctx, cursor, "queue:*", 100).Result(); on error return 500 with gin.H{"error": "redis scan failed"}; for each key strip "queue:" prefix (k[len("queue:"):]); cursor = next; if cursor == 0 break }
    - After loop: if eventIDs == nil { eventIDs = []string{} } — never return null
    - c.JSON(http.StatusOK, gin.H{"events": eventIDs})
    - Add file-level comment: "// GetEvents handles GET /queue/events.\n// Returns eventIds from Redis SCAN on queue:* keys.\n// ponytail: SCAN over KEYS — safe on large keyspaces (D-09)."

    **internal/api/router.go:** Add r.GET("/queue/events", h.GetEvents) after the existing r.GET("/queue/config/:eventId", h.GetConfig) line. One line addition.
  </action>
  <verify>
    <automated>cd /Users/aditya.bansal@zomato.com/projects/virtual-queue && go build ./... && go vet ./internal/api/ && grep -c "GetEvents" internal/api/router.go && grep -c "SCAN" internal/api/handler_events.go</automated>
  </verify>
  <acceptance_criteria>
    - internal/api/handler_events.go exists, package api, has GetEvents method on *Handler
    - handler_events.go uses h.rdb.Scan (not h.rdb.Keys) — grep -c "Scan" handler_events.go returns >= 1
    - handler_events.go has the nil guard: eventIDs = []string{} when slice is nil
    - internal/api/router.go has r.GET("/queue/events", h.GetEvents) line
    - go build ./... exits 0
    - go vet ./internal/api/ exits 0
    - curl http://localhost:8080/queue/events (when stack is up) returns 200 JSON with "events" key
  </acceptance_criteria>
  <done>GET /queue/events is registered and returns {events:[]} when no queue keys exist, or a list of eventId strings when queues are active. go build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Admin dashboard HTML, JS, and CSS with full event lifecycle</name>
  <files>web/admin/index.html, web/admin/admin.js, web/admin/admin.css</files>
  <read_first>
    - .planning/phases/02-frontend-admin-ui/02-UI-SPEC.md (Component Inventory for Admin Dashboard, State Machine table, Copywriting Contract, Layout Contract, Color/Typography tokens)
    - .planning/phases/02-frontend-admin-ui/02-PATTERNS.md (admin.js polling pattern, event selector pattern, rate update pattern, QUEUE_CONFIG injection pattern, admin.html DOM ids)
    - web/queue/index.html (sibling pattern for window.QUEUE_CONFIG injection structure)
    - web/queue/queue.css (sibling pattern for design tokens, will not be modified)
  </read_first>
  <action>
    **web/admin/index.html:** Full HTML per UI-SPEC layout contract:
    - DOCTYPE, charset=utf-8, viewport meta
    - title: "Queue Admin"
    - First script tag in head: window.QUEUE_CONFIG = { apiBase: 'http://localhost:8080' } per D-02
    - Link admin.css, script src="admin.js" defer
    - Two-region layout per UI-SPEC:
      - div.top-bar: contains select#event-select and span#poll-indicator
      - div.content: contains div.stat-grid (6 stat cards) and div.edit-controls (form)
    - Stat cards (each div.stat-card): Queue Depth (span#stat-depth), Active Users (span#stat-active), Admit Rate (span#stat-rate), Capacity (span#stat-capacity), Headroom (span#stat-headroom), Est. Drain (span#stat-drain)
    - Each stat-card has a value span (display size) and a label span (label size)
    - Edit controls: form#update-form with label + input#rate-input (type=number min=1) + label + input#capacity-input (type=number min=1) + button#update-btn "Update Config"
    - poll-indicator: small colored dot, blue (#2563eb) when polling active (class .active), gray (#6b7280) when idle
    - event-select: first option value="" "No events active" (shown when no events loaded)
    - update-btn: disabled attribute when no event selected

    **web/admin/admin.js:** Full implementation per PATTERNS.md patterns. All functions:
    - window.QUEUE_CONFIG.apiBase for all fetch URLs (never hardcoded)
    - let pollTimer = null, currentEventId = null
    - loadEvents(): fetch QUEUE_CONFIG.apiBase/queue/events → parse {events}; populate event-select options (remove all except the placeholder first); if events.length === 0 show "No events active" and set stat cards to "—"; else add options and call startPolling(events[0]) for the first event; update-btn disabled when currentEventId is null
    - startPolling(eventId): clear existing pollTimer, set currentEventId = eventId, enable update-btn, set poll-indicator to active (add class 'active'), call fetchConfig() immediately, set pollTimer = setInterval(fetchConfig, 2000)
    - fetchConfig(): fetch QUEUE_CONFIG.apiBase/queue/config/currentEventId; on success call renderStats(data); on error (non-200 or throw) → append "(stale)" to each stat card current value (only if a value exists — don't show "(stale)" on initial "—" state)
    - renderStats(data): update each stat span: stat-depth = data.queueDepth, stat-active = data.activeUsers, stat-rate = data.admitRate, stat-capacity = data.capacity; headroom = (data.capacity > 0) ? (data.capacity - data.activeUsers) : "—"; stat-headroom = headroom; stat-drain = (data.estimatedDrainSec >= 0) ? Math.ceil(data.estimatedDrainSec / 60) + " min" : "—"; apply accent color on #stat-headroom when headroom is a number and headroom < (data.capacity * 0.1)
    - form#update-form submit handler: preventDefault; if !currentEventId return; rate = parseInt(rate-input.value); capacity = parseInt(capacity-input.value); if isNaN(rate) || rate < 1 return; fetch PUT QUEUE_CONFIG.apiBase/queue/rate/currentEventId with {rate, capacity} JSON body; on 200: update-btn.textContent = "Saved", setTimeout(() => update-btn.textContent = "Update Config", 1500); on non-200: show error element "Update failed. Try again." below the button; re-enable button
    - document.getElementById('event-select').addEventListener('change', e => { if (e.target.value) startPolling(e.target.value); })
    - Call loadEvents() on DOMContentLoaded

    **web/admin/admin.css:** Per UI-SPEC layout contract and design tokens:
    - Body: font-family system-ui,-apple-system,sans-serif; font-size 16px; background #ffffff; margin 0
    - .top-bar: background #f3f4f6; padding 16px 24px; display flex; align-items center; gap 16px
    - .content: padding 24px
    - .stat-grid: display grid; grid-template-columns repeat(3, 1fr); gap 16px; margin-bottom 24px; on screens < 768px: grid-template-columns repeat(2, 1fr)
    - .stat-card: background #ffffff; border 1px solid #e5e7eb; border-radius 8px; padding 24px
    - .stat-value: font-size 28px; font-weight 600; line-height 1.1; display block; overflow-wrap break-word; min-width 0
    - .stat-label: font-size 14px; color #6b7280; display block; margin-top 4px
    - .stat-value.accent: color #2563eb (used on headroom when < 10% of capacity)
    - .edit-controls: display flex; align-items center; gap 8px; flex-wrap wrap
    - input[type=number]: padding 6px 8px; border 1px solid #e5e7eb; border-radius 4px; font-size 16px; width 80px
    - #update-btn: background #2563eb; color #ffffff; border none; border-radius 4px; padding 8px 16px; cursor pointer; font-size 16px
    - #update-btn:disabled: opacity 0.5; cursor not-allowed
    - #poll-indicator: width 10px; height 10px; border-radius 50%; background #6b7280; display inline-block
    - #poll-indicator.active: background #2563eb
    - #update-error: color #dc2626; font-size 14px; margin-top 8px (hidden by default via JS)
    - @media (max-width: 768px): .stat-grid grid-template-columns repeat(2, 1fr)
  </action>
  <verify>
    <automated>grep -c "QUEUE_CONFIG" /Users/aditya.bansal@zomato.com/projects/virtual-queue/web/admin/admin.js && grep -c "loadEvents" /Users/aditya.bansal@zomato.com/projects/virtual-queue/web/admin/admin.js && grep -c "startPolling" /Users/aditya.bansal@zomato.com/projects/virtual-queue/web/admin/admin.js && grep -c "setInterval" /Users/aditya.bansal@zomato.com/projects/virtual-queue/web/admin/admin.js && grep -c "Update Config" /Users/aditya.bansal@zomato.com/projects/virtual-queue/web/admin/admin.js</automated>
    <human-check>Run docker compose up. Open http://localhost:8082/admin/ — event selector should populate with active events. Select an event, confirm stat cards update every 2 seconds. Change rate to 5, click Update Config, confirm button shows "Saved" for ~1.5s.</human-check>
  </verify>
  <acceptance_criteria>
    - web/admin/index.html exists with event-select, stat-depth, stat-active, stat-rate, stat-capacity, stat-headroom, stat-drain, rate-input, capacity-input, update-btn, poll-indicator DOM ids
    - web/admin/admin.js: zero occurrences of the literal string "localhost:8080" — all URLs via QUEUE_CONFIG.apiBase
    - admin.js contains loadEvents, startPolling, fetchConfig, renderStats functions
    - admin.js contains form submit handler with PUT /queue/rate/${currentEventId}
    - admin.js contains "Saved" button label (1.5s transient) per copywriting contract
    - admin.js contains "Update failed. Try again." error copy per copywriting contract
    - admin.js contains "(stale)" appended to stat values on poll failure per UI-SPEC
    - admin.css has .stat-grid with grid-template-columns and @media query for < 768px
    - admin.css has #update-btn with background #2563eb (accent color)
    - admin.css has overflow-wrap break-word on .stat-value (backstop for large numbers)
  </acceptance_criteria>
  <done>Admin dashboard at http://localhost:8082/admin/ shows live stats for the selected event. Rate/capacity update takes effect on the next scheduler tick. All UI-SPEC admin states are handled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→GET /queue/events | Internal-only admin endpoint; no auth by design (D-06) |
| browser→PUT /queue/rate | Rate/capacity mutation; no auth; local dev only (D-06 deferred) |
| Redis SCAN→event list | Server-side data discovery; scan pattern bounded to queue:* namespace |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Elevation | GET /queue/events — no auth | low | accept | Local Docker Compose only (D-06); comment in handler: "// ponytail: no auth — add bearer token or IP allowlist before production" |
| T-02-06 | Tampering | PUT /queue/rate — no auth | low | accept | Local Docker Compose only (D-06); same comment pattern as handler_admin.go T-04-01 |
| T-02-07 | Denial of Service | Redis SCAN loop in GetEvents | low | mitigate | SCAN with count=100 and cursor loop is non-blocking; bounded by keyspace size; acceptable for local dev |
| T-02-08 | Information Disclosure | event selector exposes all active event IDs | low | accept | eventIds are operational identifiers, not secrets; exposed to admin dashboard consumers only |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages; vanilla HTML/JS only |
</threat_model>

<verification>
After both tasks complete:

1. go build ./... exits 0
2. go vet ./internal/api/ exits 0
3. GET /queue/events endpoint registered: grep -c "GetEvents" internal/api/router.go returns 1
4. SCAN used not KEYS: grep -c "\.Keys(" internal/api/handler_events.go returns 0
5. No hardcoded URLs in admin.js: grep -v "^//" web/admin/admin.js | grep -c "localhost:8080" returns 0
6. Admin dashboard files exist: ls web/admin/{index.html,admin.js,admin.css}
</verification>

<success_criteria>
- GET /queue/events returns 200 with {events:[]} when stack is fresh
- Admin dashboard loads at http://localhost:8082/admin/ without JS errors
- Event selector populates when events are active in Redis
- Stats update live every 2 seconds
- Rate/capacity update reflects on next scheduler tick
- Empty event state shows "No events active" and stat cards show "—"
</success_criteria>

<artifacts_produced>
Files created by this plan:
- internal/api/handler_events.go — GetEvents handler, Redis SCAN on queue:* keys
- web/admin/index.html — admin dashboard HTML with two-region layout
- web/admin/admin.js — full admin polling, event selector, rate update logic
- web/admin/admin.css — two-region layout, stat grid, form styling
Files modified:
- internal/api/router.go — GET /queue/events route added
</artifacts_produced>

<output>
Create `.planning/phases/02-frontend-admin-ui/02-02-SUMMARY.md` when done.
</output>
