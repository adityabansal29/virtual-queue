---
phase: 03-aws-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/modules/networking/main.tf
  - infra/modules/networking/variables.tf
  - infra/modules/networking/outputs.tf
  - infra/environments/dev/main.tf
  - infra/environments/dev/variables.tf
  - infra/environments/dev/terraform.tfvars
  - infra/environments/dev/backend.tf
  - infra/modules/ecs/ecr.tf
  - infra/modules/ecs/variables.tf
  - infra/modules/ecs/outputs.tf
autonomous: true
requirements:
  - INFRA-02

estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-02: Terraform HCL with AWS provider targets ap-south-1; `terraform validate` passes"
    - "D-03: Remote state backend points to a manually-bootstrapped S3 bucket; no DynamoDB lock table"
    - "D-04: Module structure exists at infra/modules/{networking,ecs,...} and infra/environments/dev/"
    - "D-06: Three ECR repositories exist — one each for queueserver, scheduler, stuborigin"
    - "VPC, two public subnets, and two private subnets exist in ap-south-1 with correct CIDR blocks"
    - "`terraform plan` in infra/environments/dev/ produces a non-empty plan with no errors"
  artifacts:
    - infra/modules/networking/main.tf
    - infra/modules/networking/outputs.tf
    - infra/modules/ecs/ecr.tf
    - infra/environments/dev/main.tf
    - infra/environments/dev/backend.tf
    - infra/environments/dev/terraform.tfvars
  key_links:
    - "networking module outputs (vpc_id, subnet_ids, security_group_ids) consumed by ecs/redis/cloudfront modules in 03-02"
    - "ECR repo URLs output by ecs module consumed by GitHub Actions in 03-03"
---

<objective>
Tracer plan: lay the Terraform skeleton end-to-end — remote state backend, module directory structure, networking module (VPC/subnets/SGs), and ECR repositories. This is the one path that proves every layer of the infrastructure stack can be reached from `terraform plan` before any compute or data resources are built.

D-01: Phase 3 is split into 3a (core compute + data, plans 01-03) and 3b (edge + content delivery, plans 04-05). This plan is the Wave 1 tracer for 3a; 3b plans depend on 3a completing.

Purpose: Validates IaC toolchain, region, module wiring, and state backend before any billable resources are created. All subsequent plans consume outputs from this one.
Output: `infra/` directory tree, networking resources plannable/applyable, three ECR repos created.
</objective>

<execution_context>
@/Users/aditya.bansal@zomato.com/projects/virtual-queue/.claude/gsd-core/workflows/execute-plan.md
@/Users/aditya.bansal@zomato.com/projects/virtual-queue/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-aws-infrastructure/03-CONTEXT.md
</context>

<tasks>

<task type="tracer">
  <name>Terraform skeleton — remote backend + module structure + networking module</name>
  <files>
    infra/environments/dev/backend.tf,
    infra/environments/dev/variables.tf,
    infra/environments/dev/terraform.tfvars,
    infra/environments/dev/main.tf,
    infra/modules/networking/main.tf,
    infra/modules/networking/variables.tf,
    infra/modules/networking/outputs.tf,
    infra/modules/ecs/variables.tf,
    infra/modules/ecs/outputs.tf,
    infra/modules/redis/variables.tf,
    infra/modules/redis/outputs.tf,
    infra/modules/cloudfront/variables.tf,
    infra/modules/cloudfront/outputs.tf,
    infra/modules/s3/variables.tf,
    infra/modules/s3/outputs.tf,
    infra/modules/dynamodb/variables.tf,
    infra/modules/dynamodb/outputs.tf,
    infra/modules/sqs/variables.tf,
    infra/modules/sqs/outputs.tf
  </files>
  <action>
    Create the Terraform directory tree per D-04:

    ```
    infra/
      modules/
        ecs/          — ecr.tf + stub main.tf, variables.tf, outputs.tf
        redis/        — stub main.tf, variables.tf, outputs.tf
        cloudfront/   — stub main.tf, variables.tf, outputs.tf
        s3/           — stub main.tf, variables.tf, outputs.tf
        dynamodb/     — stub main.tf, variables.tf, outputs.tf
        sqs/          — stub main.tf, variables.tf, outputs.tf
        networking/   — fully implemented (see below)
      environments/
        dev/
          backend.tf
          variables.tf
          terraform.tfvars
          main.tf
    ```

    All stub modules (ecs except ecr.tf, redis, cloudfront, s3, dynamodb, sqs) need only an empty `main.tf`, a `variables.tf` with the inputs they will eventually accept, and an `outputs.tf` with the outputs they will eventually expose. Stubs must be valid HCL so `terraform validate` passes.

    **backend.tf** (per D-03 — S3 only, no DynamoDB lock):
    ```hcl
    terraform {
      backend "s3" {
        bucket = var.tf_state_bucket   # set in terraform.tfvars
        key    = "dev/terraform.tfstate"
        region = "ap-south-1"
      }
    }
    ```
    Note: backend block cannot use variables; use partial backend config (`-backend-config` flag on init) or hardcode the bucket name from terraform.tfvars as a literal string. Use a `-backend-config=backend.hcl` approach: create `infra/environments/dev/backend.hcl` with `bucket = "..."` and document `terraform init -backend-config=backend.hcl` in a comment at the top of backend.tf.

    **variables.tf**: declare `aws_region` (default `"ap-south-1"`), `environment` (default `"dev"`), `tf_state_bucket`, `vpc_cidr` (default `"10.0.0.0/16"`).

    **terraform.tfvars**: set `aws_region = "ap-south-1"`, `environment = "dev"`. Leave `tf_state_bucket` empty with a `# FILL IN` comment — the developer sets this after bootstrapping the state bucket manually (per D-03).

    **main.tf** (dev environment root):
    - `terraform {}` block with `required_providers { aws = { source = "hashicorp/aws", version = "~> 5.0" } }`
    - `provider "aws" { region = var.aws_region }`
    - `module "networking" { source = "../../modules/networking"; ... }` (pass vpc_cidr, environment)
    - Stub module calls for ecs, redis, dynamodb, sqs, s3, cloudfront — all with `source` pointing to their module paths; comment each with `# implemented in 03-02` or `# implemented in 03-04/03-05`.

    **networking/main.tf** — fully implement (per D-02 discretion for CIDR/NAT):
    - `aws_vpc` with cidr_block = var.vpc_cidr, enable_dns_support, enable_dns_hostnames
    - Two public subnets in ap-south-1a and ap-south-1b (10.0.1.0/24, 10.0.2.0/24)
    - Two private subnets in ap-south-1a and ap-south-1b (10.0.10.0/24, 10.0.11.0/24)
    - `aws_internet_gateway` attached to VPC
    - `aws_nat_gateway` in one public subnet (single NAT for dev cost savings — Claude's discretion)
    - Route tables: public subnets → IGW; private subnets → NAT GW
    - Security groups: `sg_alb_public` (ingress 80/443 from 0.0.0.0/0), `sg_ecs_tasks` (ingress from sg_alb_public on 8080/8081), `sg_redis` (ingress on 6379 from sg_ecs_tasks), `sg_alb_stub_origin` (ingress from CloudFront managed prefix list — wired in 03-05; placeholder with ingress from sg_alb_public for now)
    - Tag all resources with `Environment = var.environment`

    **networking/outputs.tf**: output `vpc_id`, `public_subnet_ids` (list), `private_subnet_ids` (list), `sg_alb_public_id`, `sg_ecs_tasks_id`, `sg_redis_id`, `sg_alb_stub_origin_id`.
  </action>
  <verify>
    <automated>cd /Users/aditya.bansal@zomato.com/projects/virtual-queue/infra/environments/dev && terraform init -backend=false && terraform validate</automated>
  </verify>
  <done>
    `terraform validate` exits 0 with "Success! The configuration is valid." All module directories exist. networking/main.tf has VPC + subnets + SGs. backend.tf documents the init command. terraform.tfvars has FILL IN comment for state bucket.
  </done>
</task>

<task type="auto">
  <name>ECR repositories — one per service (per D-06)</name>
  <files>
    infra/modules/ecs/ecr.tf,
    infra/modules/ecs/outputs.tf
  </files>
  <action>
    In `infra/modules/ecs/ecr.tf`, create three `aws_ecr_repository` resources:
    - `queueserver` — name = "${var.environment}-queueserver", image_tag_mutability = "MUTABLE", force_delete = true (dev convenience)
    - `scheduler`   — name = "${var.environment}-scheduler"
    - `stuborigin`  — name = "${var.environment}-stuborigin"

    Add a lifecycle policy to each via `aws_ecr_lifecycle_policy` that keeps only the last 10 images (standard `countType=imageCountMoreThan, countNumber=10` JSON policy). This prevents unbounded storage growth on repeated pushes.

    In `infra/modules/ecs/outputs.tf`, output:
    - `ecr_queueserver_url` = `aws_ecr_repository.queueserver.repository_url`
    - `ecr_scheduler_url`   = `aws_ecr_repository.scheduler.repository_url`
    - `ecr_stuborigin_url`  = `aws_ecr_repository.stuborigin.repository_url`

    In `infra/environments/dev/main.tf`, update the `module "ecs"` stub call to pass `environment = var.environment` so the ECR names are namespaced.
  </action>
  <verify>
    <automated>cd /Users/aditya.bansal@zomato.com/projects/virtual-queue/infra/environments/dev && terraform validate</automated>
  </verify>
  <done>
    `terraform validate` still passes. `infra/modules/ecs/ecr.tf` defines three `aws_ecr_repository` resources and three `aws_ecr_lifecycle_policy` resources. ECR URLs are exported as module outputs.
  </done>
</task>

<task type="auto">
  <name>Terraform lint + plan dry-run documentation</name>
  <files>
    infra/environments/dev/README.md
  </files>
  <action>
    Create `infra/environments/dev/README.md` documenting the bootstrap sequence a developer must follow before running `terraform apply` for the first time:

    1. Create the Terraform state S3 bucket manually in ap-south-1 (per D-03 — no lock table):
       ```
       aws s3api create-bucket \
         --bucket <your-unique-state-bucket-name> \
         --region ap-south-1 \
         --create-bucket-configuration LocationConstraint=ap-south-1
       aws s3api put-bucket-versioning \
         --bucket <your-unique-state-bucket-name> \
         --versioning-configuration Status=Enabled
       ```
    2. Create `infra/environments/dev/backend.hcl`:
       ```
       bucket = "<your-unique-state-bucket-name>"
       ```
    3. Run `terraform init -backend-config=backend.hcl`
    4. Run `terraform plan` — should show networking resources and ECR repos
    5. Run `terraform apply`

    Also note that AWS credentials must be set via environment variables or `~/.aws/credentials` before any Terraform commands.

    Document expected outputs after first apply: VPC ID, subnet IDs, ECR repo URLs.

    Do NOT create a markdown file anywhere else. This README is part of the `files_modified` list for this plan.
  </action>
  <verify>
    <automated>ls /Users/aditya.bansal@zomato.com/projects/virtual-queue/infra/environments/dev/README.md && grep -c "terraform init -backend-config" /Users/aditya.bansal@zomato.com/projects/virtual-queue/infra/environments/dev/README.md</automated>
  </verify>
  <done>
    `infra/environments/dev/README.md` exists and documents the 5-step bootstrap sequence including manual state bucket creation, backend.hcl creation, and terraform init/plan/apply.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation → AWS | Terraform apply credentials must be restricted to least-privilege IAM role |
| public internet → ECR | ECR repos are private; image pull requires AWS auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | Terraform state S3 bucket | high | mitigate | Enable S3 versioning on state bucket (documented in README bootstrap steps); loss of state is catastrophic |
| T-03-02 | Information Disclosure | Terraform state file | medium | mitigate | State bucket is private, no public access; sensitive values (secrets) must not be written into state — secrets come from SSM (D-07), not Terraform outputs |
| T-03-03 | Elevation of Privilege | IAM credentials for Terraform | high | mitigate | Developer uses dedicated IAM role/user with least-privilege policy scoped to this project; credentials never committed to repo |
| T-03-SC | Tampering | No npm/pip/cargo installs in this plan | low | accept | No package manager installs; Terraform provider downloads are hashed by Terraform lockfile |
</threat_model>

<verification>
Run from project root after `terraform apply` (or in dry-run with `-backend=false`):

```bash
cd infra/environments/dev
terraform init -backend=false
terraform validate
# Should print: "Success! The configuration is valid."

# Structural check — all module directories exist
ls infra/modules/networking/main.tf
ls infra/modules/ecs/ecr.tf
ls infra/modules/redis/main.tf
ls infra/modules/dynamodb/main.tf
ls infra/modules/sqs/main.tf
ls infra/modules/s3/main.tf
ls infra/modules/cloudfront/main.tf
```
</verification>

<success_criteria>
- `terraform validate` passes with zero errors
- All 7 module directories exist under infra/modules/
- networking module has VPC + 4 subnets + IGW + NAT GW + 4 SGs
- ecs module has 3 ECR repos with lifecycle policies
- infra/environments/dev/ has backend.tf, variables.tf, terraform.tfvars, main.tf, README.md
- D-02 (Terraform, ap-south-1), D-03 (S3 backend, no lock), D-04 (module structure), D-06 (3 ECR repos) all satisfied
</success_criteria>

<output>
Create `.planning/phases/03-aws-infrastructure/03-01-SUMMARY.md` when done
</output>
